{
  "compilerOptions": {
    "module": "CommonJS",
    "lib": ["ESNext"], // TS需要引用的库
    "outDir": "./dist", // 指定输出目录
    "rootDir": "./", // 指定输出文件目录(用于输出)，用于控制输出目录结构
    "esModuleInterop": true // 允许export=导出，由import from 导入
  }
}
